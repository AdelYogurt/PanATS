% Mach number (non-dimensional, based on the free-stream values)
MACH_NUMBER = 13.8;
% Free-stream pressure (101325.0 N/m^2, 2116.216 psf by default)
FREESTREAM_PRESSURE = 951.5;
% Free-stream temperature (288.15 K by default)
FREESTREAM_TEMPERATURE = 89.3;
% Angle of attack (degrees), default is 0.0
AOA = 0.0;
% Side-slip angle (degrees, only for compressible flows), default is 0.0
SIDESLIP_ANGLE = 0.0;

% Air ratio of Specific Heat
GAMA_VALUE = 1.4;

% Navier-Stokes wall boundary marker(s) (NONE = no marker) constant wall temperature
MARKER_ISOTHERMAL = 300;

% Reference origin for moment computation
REF_ORIGIN_MOMENT_X = 0.125;
REF_ORIGIN_MOMENT_Y = 0.00;
REF_ORIGIN_MOMENT_Z = 0;
% Reference length for pitching, rolling, and yawing non-dimensional moment
REF_LENGTH = 0.177;
% Reference area for force coefficients (0 implies automatic calculation)
REF_AREA = 1;

% Symmetry faca
SYMMETRY = ZOX;
% SYMMETRY = ();

% Marker(s) of the surface where the functional (Cd, Cl, etc.) will be evaluated, The STL format can be left blank
% MARKER_MONITORING = { 'waverider_panel' };
% Mesh input file
% MESH_FILENAME = 'waverider_panel';
% Mesh input file format (SU2, STL, INP)
% MESH_FORMAT =  'SU2'

% Mesh zoom size, mesh input data will be zoom by MESH_SCALE
% MESH_SCALE = 1;
% Mesh input file
% MESH_FILENAME = 'waverider_temp';
% Mesh input file format (SU2, STL, INP)
% MESH_FORMAT =  'STL'

% Mesh zoom size, mesh input data will be zoom by MESH_SCALE
MESH_SCALE = 0.001;
% Mesh input file
MESH_FILENAME = 'waverider_cone';
% Mesh input file format (SU2, STL, INP)
MESH_FORMAT =  'STL'

% Mesh input file
% MESH_FILENAME = 'BWB';
% Mesh input file format (SU2, STL, INP)
% MESH_FORMAT =  'STL'